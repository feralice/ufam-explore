import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 20,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: 50,
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 20,
    color: 'darkblue',
  },
  backButton: {
    position: 'absolute',
    top: 55,
    left: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
